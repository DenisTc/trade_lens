name: ai_review
description: "TradeLens · the pull-request reviewer: a diff, one Claude call with structured output, one comment."
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.13.0
dependencies:
  ai_insights:
  core:
  meta: ^1.16.0
dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^11.0.0
