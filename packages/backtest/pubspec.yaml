name: backtest
description: "TradeLens · grid and DCA backtests over historical candles: trades, metrics, equity curve. Pure Dart."
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.13.0
dependencies:
  core:
  domain:
  meta: ^1.16.0
  rational: ^2.0.0
dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^11.0.0
