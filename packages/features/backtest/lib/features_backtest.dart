/// TradeLens · feature "backtest": a grid or a DCA bot run over the
/// candles the pair screen already holds, its trades on the chart, and
/// two parameter sets side by side. The engine is `package:backtest`;
/// this package is the form, the run and the figures.
library;

export 'src/backtest_screen.dart';
export 'src/backtest_setup.dart';
