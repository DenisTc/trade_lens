/// TradeLens · remote configuration. Firebase Remote Config behind the
/// domain `InsightsConfigSource`, plus a defaults-only source for builds
/// without Firebase.
library;

export 'src/remote_config_client.dart';
export 'src/remote_insights_config_source.dart';
